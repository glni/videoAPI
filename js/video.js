$(document).ready(function () {

    let current_time = 0;
    const $my_video = $("#about_video");
    const $c_welcome = $("#content_welcome");
    const $c_coffee = $("#content_coffee");
    const $c_baked = $("#content_baked_goods");
    const $c_promise = $("#content_promise");
    const $allAsides = $("aside");

    //Eventlistner til når videoen afspiller "timeupdate"

        // [0] is selecting the DOM element instead of a jQuery Element

        //current_time < 8.75

        //current_time > 8.75

        //current_time > 22.4

        //current_time > 29.2

        //current_time > 45


});